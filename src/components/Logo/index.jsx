import { Link } from 'react-router-dom';

const Logo = () => {
    return (
        <Link href="home">
            <img src="logo192.png" alt="Logo Kasa" />
        </Link>
    );
};

export default Logo;
