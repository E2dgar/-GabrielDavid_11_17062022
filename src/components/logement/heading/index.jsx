import FlexBetweenContainer from '../../Layouts/flex/flexBetween';
import './index.css';

const LogementHeading = ({ children, className }) => {
    return;
    <FlexBetweenContainer className={className}>
        {children}
    </FlexBetweenContainer>;
};

export default LogementHeading;
